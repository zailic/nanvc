version: '2'
services:

  vault:
    container_name: vault.server
    command: server
    image: vault:latest
    volumes:
      - ./test/util/vault/config:/vault/config
    cap_add:
      - IPC_LOCK
    ports:
      - "8200:8200"